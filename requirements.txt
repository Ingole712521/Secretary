# Jarvis OS — Phase 0 Foundation Dependencies
# Python 3.12+
#
# Install: pip install -r requirements.txt
#
# Dev tooling (ruff, black, mypy, pytest) will be added in pyproject.toml
# during Phase 1 setup. Keep this file limited to runtime dependencies only.

# API Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# Validation & Configuration
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Async HTTP Client
httpx>=0.28.0,<1.0.0

# Desktop automation (Windows primary — Sprint 6+)
pyautogui>=0.9.54,<1.0.0
pygetwindow>=0.0.9,<1.0.0

# Voice (Sprint 8+)
edge-tts>=6.1.0,<7.0.0
